IDEE FUTURE A AJOUTER OU PAS A WEBSERV

- faire un fichier de log de serveur (log.txt ou croissant.txt) et rediriger les erreurs dans ce fichier
- dans la fonction Answer::radnmName ajoute un message de log sur le fichier de log qui dit que 
	le nom du fichier a uplad n'a pas ete entre correctement et donc a ete prit alleatoiremet puis a le mime .txt


to do 

	y'a un fonction handle qui est pas mise dans le ptr sur fct

	class:
	constructeur de copy de conf (OPTIONNEL)

	Dans le parsing :


	cgi dans le parsing mettre le chemin des script accepte + mettre un timeout sur l'acceptation des script belek au script avec une boucle infinie 502.
